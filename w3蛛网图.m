clear;clc;
figure;
x=0:0.01:1;
r=3.5;
y1=x;
y2=r*x.*(1-x);
plot(x,y1);
hold on;
plot(x,y2);
%%
x=0.2;
n=1000;
box=ones(1,n)*0;  %给box一个形状
for i=1:n  %for不需要分号
    box(i)=x;
    x=r*x*(1-x);
end
B=box;
A=repmat(B,2,1); % A=[B;B];简单可以用，默认按列展开，如果按行，要转置一下  A'   %B，2,1指造一个两行一列的矩阵，把B放在里面
C=A(:);   %矩阵没有行展开，只有列展开，就是按照列的顺序走，不然就转置一下
D=C.';  %矩阵里有复数，就要用.' 不然会变成它的复共轭 
E=C;
E(1)=[];
C(2*n)=[];  %必须要*
m=500;
E(1:m)=[];
C(1:m)=[];
plot(C,E);


